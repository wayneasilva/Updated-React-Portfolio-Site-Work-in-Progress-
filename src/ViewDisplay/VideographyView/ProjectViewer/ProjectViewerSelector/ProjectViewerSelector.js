import React from 'react';
import Radium from 'radium';

const projectViewerSelector = (props) => {
    return(
        <div>Project Viewer Selector</div>
    )
}

export default Radium(projectViewerSelector);